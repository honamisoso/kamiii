<div class="container">
  <h2>ユーザー名</h2>
  <%= @user.name %>

  <h2>自己紹介</h2>
  <%= simple_format(@user.introduction, samitize: true, class: 'm-0') %>

  <% if @user == current_user %>
    <%= link_to '編集', edit_user_path(@user), class: 'btn btn-sm btn-info' %>
  <% end %>
</div>